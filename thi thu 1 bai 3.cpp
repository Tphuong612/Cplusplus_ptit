#include <bits/stdc++.h>
using namespace std;
struct sv{
	string ma, ten, ok;
	int sl=0;
};
sv a[1005];
set <string> s;
void solve()
	{
		string ng, tg, msv, ten, dc;
		cin>>ng>>tg>>msv>>dc;
		if(dc!=check) 
			{
				for(int i=0;i<n;i++)
					{
						if(msv==a[i]) 
							{
								a[i].sl=1;
								a[i].ok="FAIL";
								break;
							}
					}
			}
	}
int main()
{
	int n;
	cin>>n;
	cin.ignore();
	
	for(int i=0;i<n;i++)
		{
			getline(cin, ma);
			s.insert(ma);
			getline(cin, ten);
		}
	
	int m;
	cin>>m;
	cin.ignore();
	string check="code.ptit.edu.vn";
	while(m--)
		{
		
		}
	
	
	
	
	
	
}
